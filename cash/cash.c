#include<stdio.h>
char Input[256];
float chartofloat(char *s)
{
  float res=0;
  int sign=1;
  if((s[0]>=0)&&(s[0]<=9)||(s[0]==',')||(s[0]=='-'))
  {
    
}
float get_float(char *message)
{
  printf("%s",massage);
  gets(&Input);
  return chartofloat(Input);
}

int main()
{
  
}
